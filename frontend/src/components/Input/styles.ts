import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    inputStyle: {
        borderWidth: 1,
        borderColor: "#ccc",
        padding: 12,
        borderRadius: 8,
        marginTop: 8,
    }
});